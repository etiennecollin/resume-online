# online-resume

My online resume.

## Requirements

```bash
cargo install trunk
```

For dev:

```bash
trunk serve --address 0.0.0.0 --open
```

For release:

```bash
trunk build --release -d docs
```
